spring.application.name=firstproj

spring.datasource.driver-class-name=org.postgresql.Driver
# for when running the backend in a docker container
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# for when running the backend normally in the terminal
spring.datasource.url=jdbc:postgresql://localhost:5432/testdb
spring.datasource.username=akul
spring.datasource.password=Nalakoi15

# for when running the db inside a docker container
# spring.datasource.url=jdbc:postgresql://localhost:5433/testdb
# spring.datasource.username=akul
# spring.datasource.password=Nalakoi15

spring.jpa.hibernate.ddl-auto=update
